import { default } from 'react';
import * as React from 'react';

export declare const ButtonWithDefault: default.FC = ({ children }) => (
  <button>{children}</button>
);

export declare const ButtonWithStar: React.FC = ({ children }) => (
  <button>{children}</button>
);

export { }
